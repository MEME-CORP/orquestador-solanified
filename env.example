# SUPABASE CONFIGURATION
# Copy this file to .env and replace the placeholder values with your actual Supabase credentials

# Your Supabase project URL (found in Project Settings > API)
# REQUIRED: Set this in Render dashboard
SUPABASE_URL=https://your-project-id.supabase.co

# Your Supabase anon/public key (found in Project Settings > API)
# REQUIRED: Set this in Render dashboard
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-anon-key-here

# Supabase Database Password (for direct PostgreSQL connections)
# REQUIRED FOR BUNDLER OPERATIONS: Needed for atomic transactions in bundler creation
# Found in Supabase Project Settings > Database > Connection pooling
# Without this, the system will fall back to Supabase client (less reliable for concurrent operations)
SUPABASE_DB_PASSWORD=your-database-password-here

# Direct Database Connection String (alternative to constructed connection)
# OPTIONAL: Use this if you want to provide the full connection string instead of SUPABASE_DB_PASSWORD
# Format: postgresql://postgres:password@db.project-id.supabase.co:5432/postgres
# DATABASE_URL=postgresql://postgres:password@db.project-id.supabase.co:5432/postgres


# EXTERNAL API CONFIGURATION
# Base URL for the blockchain primitives API
# REQUIRED: Set this in Render dashboard
# NOTE: Don't include port numbers for Render services - they handle ports internally
EXTERNAL_API_BASE_URL=https://rawapisolana-render.onrender.com


# FRONTEND CONFIGURATION
# Frontend URL for sending notifications
# REQUIRED: Set this in Render dashboard
FRONTEND_URL=https://frontend-solanified.vercel.app

# SERVER CONFIGURATION
# Port for the orchestrator API to run on (Render will set this automatically)
PORT=3000

# Environment (development, production, test)
NODE_ENV=production

# Logging level (error, warn, info, debug)
LOG_LEVEL=info

# SECURITY CONFIGURATION
# Comma-separated list of allowed origins for CORS
# REQUIRED: Set this in Render dashboard - include your frontend domain
# Example: https://frontend-solanified.vercel.app,https://yourdomain.com
ALLOWED_ORIGINS=https://frontend-solanified.vercel.app

# RATE LIMITING
# Maximum requests per window (default: 100 per 15 minutes)
RATE_LIMIT_MAX=100

# Rate limit window in milliseconds (default: 15 minutes)
RATE_LIMIT_WINDOW_MS=900000

# PUMP.FUN BATCH PARALLELIZATION
# Size of each buy/sell batch dispatched in parallel (set to 1 to force sequential execution)
PUMP_BATCH_SIZE=10

# Delay (ms) between batches to respect rate limits when running buys/sells in parallel
PUMP_BATCH_DELAY_MS=1000


# =============================================================================
# FOR RENDER DEPLOYMENT - SET THESE ENVIRONMENT VARIABLES IN RENDER DASHBOARD:
# =============================================================================
# 1. Go to your Render service dashboard
# 2. Go to "Environment" tab
# 3. Add these REQUIRED variables with your actual values:
#
# SUPABASE_URL=https://your-actual-project-id.supabase.co
# SUPABASE_ANON_KEY=your-actual-anon-key-here
# EXTERNAL_API_BASE_URL=https://rawapisolana-render.onrender.com
# FRONTEND_URL=https://frontend-solanified.vercel.app
# ALLOWED_ORIGINS=https://frontend-solanified.vercel.app
#
# OPTIONAL (only if you need direct PostgreSQL access):
# SUPABASE_DB_PASSWORD=your-database-password-here
# OR
# DATABASE_URL=postgresql://postgres:password@db.project-id.supabase.co:5432/postgres
#
# The other variables (NODE_ENV, LOG_LEVEL, etc.) are already set in render.yaml
# =============================================================================
